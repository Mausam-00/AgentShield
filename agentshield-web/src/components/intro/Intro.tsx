"use client";

import { HudIntro } from "@/components/intro/HudIntro";

// The pre-hero intro: JARVIS-style HUD boot sequence with the centred logo.
export function Intro() {
  return <HudIntro />;
}
